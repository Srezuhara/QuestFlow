"""Demo data seeding (demo user, skill_nodes catalog, sample quests).

Stubbed in phase 0 — `make seed` is a no-op until models exist. Real
implementation lands once the relevant domain models (phase 2+) are in
place.
"""


def run() -> None:
    print("seed: not implemented yet")


if __name__ == "__main__":
    run()
